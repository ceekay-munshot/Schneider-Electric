# Dependencies
node_modules/

# Vite / Cloudflare Pages build output (Pages builds this; do not commit)
dist/
.vite/

# Capture output (uploaded as a CI artifact, not committed)
output/

# Local credentials — NEVER commit these
.env
.env.local
.env.*.local

# Playwright
playwright-report/
test-results/
/.auth/
*.har

# Logs / OS cruft
*.log
.DS_Store
